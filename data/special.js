// Special topics (locked behind coins or a Discord server). Server only:
// their exercises must never reach the browser bundle, so only server code
// may import this file. The browser gets data/special-meta.json instead,
// generated from it by scripts/special-meta.js on every build.

export const SPECIAL_LEVELS = []
