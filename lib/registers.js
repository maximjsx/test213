// Optional per-exercise tags so slang and swearing are taught with context.
// `register` says how the phrase sounds; `usage` is a short note shown after
// the learner answers.
export const REGISTERS = {
  formal:   { label: 'Formal',   color: 'var(--blue)' },
  informal: { label: 'Informal', color: 'var(--green)' },
  slang:    { label: 'Slang',    color: 'var(--orange)' },
  vulgar:   { label: 'Vulgar',   color: 'var(--red)' },
}

export const MAX_USAGE_LENGTH = 240
